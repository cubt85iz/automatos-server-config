[Unit]
Description=Target for weekly container backups
Wants={% for container in containers if container.keep_weekly is defined and container.keep_weekly > 0 %}backup-weekly@{{ container.name }}.timer {% endfor %}

[Install]
WantedBy=multi-user.target